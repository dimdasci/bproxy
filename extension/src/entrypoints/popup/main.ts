// Popup script.
//
// Task 2 lands an empty-but-loadable shell. Task 4 will replace this
// with the popup pairing flow (POST /pair/claim, payload validation,
// chrome.storage.local persistence, and pair.complete signaling).
export {};
